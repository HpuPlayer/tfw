spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tfw_db?characterEncoding=UTF-8
    username: root
    password: zx121314
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER

server:
  port: 8080

mybatis-plus:
  global-config:
    banner: false
